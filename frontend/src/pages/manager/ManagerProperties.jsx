import { PagePlaceholder } from "@/components/shared/PagePlaceholder"

export default function ManagerProperties() {
  return (
    <PagePlaceholder
      title="Properties & Units"
      role="manager"
      description="Property list + unit management + lease assignment. Built in Task #11."
    />
  )
}
