import { PagePlaceholder } from "@/components/shared/PagePlaceholder"

export default function TenantDashboard() {
  return (
    <PagePlaceholder
      title="Tenant Dashboard"
      role="tenant"
      description="Cards for current lease, next rent due, unread notifications, open maintenance requests. Built in Task #6."
    />
  )
}
